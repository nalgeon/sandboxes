{
    "run": {
        "engine": "docker",
        "entry": "main.sql",
        "steps": [
            {
                "box": "chdb",
                "command": ["python", "/opt/query.py", "main.sql"]
            }
        ]
    }
}
