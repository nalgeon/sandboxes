{
    "run": {
        "engine": "docker",
        "entry": "commands.cli",
        "steps": [
            {
                "box": "redis",
                "command": ["query.sh"]
            }
        ]
    }
}
